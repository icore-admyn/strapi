'use strict';

/**
 * permissionless-payment router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::permissionless-payment.permissionless-payment');
